import '../App.css';
import { Link } from "react-router-dom";

const Header = () => {
  return (
    <header>
      <h1>AAAAAA</h1>
      <Link to="/">
        <button>Back</button>
      </Link>
    </header>
  );
};

export default Header;
